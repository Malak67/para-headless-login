# Para API key — get from the Para Developer Portal (https://developer.getpara.com)
# Modern keys carry an env prefix so you usually don't need to set env explicitly.
VITE_PARA_API_KEY=

# WalletConnect Cloud project id — https://cloud.reown.com (formerly WalletConnect Cloud)
VITE_WALLETCONNECT_PROJECT_ID=
